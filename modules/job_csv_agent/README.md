# 自然语言招聘岗位 CSV Agent（MVP）

这个模块用 DeepSeek 做意图识别和字段抽取，用 LangGraph 管理多轮状态；只有确定性的
`CsvJobRepository` 能通过 Pandas 修改 `company_agent/data/business/jobs.csv`。新增、修改和删除
都先生成预览，只有用户显式确认后才写入。每次写入后自动重建现有 `jd_text2sql` 的 SQLite 查询库。

## 状态和必填字段

- `IDLE`：识别新建、修改、删除、查询。
- `CREATING`：累计槽位并追问。最低条件为公司名称、岗位名称，以及任职要求/岗位职责中至少一项有效岗位内容；不再为了满足必填项伪造任职要求。
- `EDITING`：模糊检索公司/岗位；多条结果先让用户选择并锁定 `job_id`。
- `CONFIRMING`：显示新增字段或修改前后值，等待确认、取消或继续修改。

在调用模型前，模块会先执行一组确定性路由规则：“我要招 / 想招 / 新建岗位 / 发布岗位”优先进入创建；只有明确的修改、删除、查询表达才会定位已有岗位。创建状态中的普通补充会继承当前草稿，因此后续输入“公司名称 + 岗位名称”不会误触发已有岗位检索。正式工、实习生、兼职和合同工只用于识别用工类型。

LLM 只能输出 Pydantic 校验的 `StructuredCommand`，不能生成或执行 CSV/SQL 写操作。
`job_id` 使用 UUID；CSV 写入使用 sidecar 文件锁和同目录临时文件原子替换。

岗位内容先进入带证据的内部 `SemanticFact`：区分职责、任职要求、技能、福利和待澄清信息，并记录重要性、来源和是否需要确认。只有用户明确提供或已确认且分类明确的事实会投影到现有 `*_json` 字符串数组列；推断建议只保留在会话草稿和确认预览中。Repository 只执行类型、必填身份字段、JSON、数值范围、未确认推断和事实/目标字段显式冲突等数据完整性硬校验，不会在用户确认保存后用低置信度关键词规则再次否决。

LLM 抽取结果、首轮创建、多轮补充、完整 JD、用户编辑后的 JSON 和最终确认预览共用同一条确定性后处理管线：标点清洗 → AND/OR 识别 → 原子条件拆分 → 忠实精简 → 类别复核 → 类别内去重 → 建议与草稿去重。`并且 / 同时 / 且 / 以及`连接的独立条件会拆分，并为共享谓语补齐“具备”等必要表达；`或 / 或者 / 任一 / 至少一种 / 二选一 / 其一`保留在同一个要求中，因此“Python 或 Java”不会被错误变成两个同时必需条件。“Spark、Flink、Doris 等工具”这类未明确 AND/OR 的列表保持原样，并产生任职要求下的确认建议。

规范化会清理 `。、`、`，、`、`、以及`、连续标点、中英文标点混用、句末句号和多余空格，统一 Linux、Python、Java、SQL、LLM 等常见技术词大小写。明确经验年限会换算到月字段，文本要求不再重复年限。完整能力句只进入任职要求；技能要求只保存简短技术标签。资格信号（具有、具备、熟悉、掌握、经验、能力、意识等）优先于句中出现的“开发 / 设计 / 代码”，只有以“负责 / 参与 / 完成 / 开展 / 推动 / 维护 / 搭建 / 交付”等构成清晰执行结构时才判为岗位职责。

字段分类后会经过轻量安全改写。该步骤只允许删除口语前缀、调整句式、展开明确缩写等忠实规范化；新增学历、年限、证书、数值、技能或增强“熟练 / 精通 / 必须 / 独立负责”等语气会被拦截或转为待确认建议。可信度由原文证据、改写等价性、类别清晰度和风险规则共同决定，不采用模型自报分数作为唯一依据。例如“后训练是训练一个招聘模型”会整理为“负责招聘领域大模型的后训练工作”，“懂 LLM”会整理为“具备大语言模型（LLM）相关基础知识”，但不会自动补入 SFT、RLHF、DPO、Python、PyTorch 或分布式训练。

预览使用严格有效的中文业务 JSON 代码块，`null` 表示未填写，列表字段始终为 JSON 数组。用户可复制该 JSON、直接编辑并发回；系统会把它识别为当前岗位草稿编辑，校验中文字段，再走同一条原子化、去重和语义检查管线，不会误判为查询或修改已有岗位，也不会直接写入 CSV。空数组表示明确清空列表；格式错误会返回行列位置并保留原草稿。

“建议补充”按岗位职责、任职要求、技能要求和工作信息分组，总数默认不超过三个。已存在于职责、要求、技能标签或标量字段中的内容不会再次建议；拒绝的建议不会在下一轮重复。建议和缺失提醒位于 JSON 代码块之外，在用户确认前不会进入准备保存的数据。

语义检查分为 `pass / warning / blocking`。未明确的工具列表、风格或低风险分类疑问仅作为预览 warning；清晰错分且无法自动修正、未确认推断即将写入、严重结构冲突才是 blocking。普通界面只显示中文说明，不暴露这些内部状态名。

最低创建条件仍是公司名称、岗位名称，以及岗位职责或任职要求至少一项。达到最低条件后即可预览；城市、办公模式、用工类型、薪资、学历、经验、技能及实习出勤等重要信息若缺失，用户可以跳过，但首次选择按当前内容保存时会收到一次完整提醒，只有再次明确确认后才写入。提醒确认后不会循环阻止保存。

## 安装与配置

```powershell
cd modules\job_csv_agent
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt
Copy-Item ..\..\.env.example ..\..\.env
# 编辑仓库根目录 .env，把 replace-me 替换为有效的 DEEPSEEK_API_KEY
```

## Chainlit 前端

先按下一节启动 FastAPI（端口 `8000`），再另开终端运行 Chainlit（端口 `8001`）：

```powershell
$env:JOB_AGENT_API_URL="http://127.0.0.1:8000"
chainlit run job_csv_agent/chainlit_app.py -w --port 8001
```

也可以直接运行已提供的启动脚本：

```powershell
.\run_chainlit.ps1
```

浏览器访问 `http://localhost:8001`。`JOB_AGENT_API_URL` 指向后端监听地址，不能把
Chainlit 自己也绑定到同一个端口。

Chainlit 只通过 HTTP 调用 FastAPI，不直接接触数据文件。确认卡片会显示“按当前内容保存 / 继续补充 / 取消”按钮；模糊检索有多个结果时会显示岗位选择按钮。普通聊天消息不会显示内部编号、存储字段名或实现状态名。

## FastAPI 后端

```powershell
uvicorn job_csv_agent.api:app --reload --port 8000
```

启动后可先访问 `http://127.0.0.1:8000/health`；`llm_configured` 必须为 `true`。

最小 API 流程：

1. `POST /sessions` 创建会话。
2. `POST /sessions/{id}/messages`，body 为 `{"content":"我想招一个 Python 开发..."}`。
3. 按返回的缺失字段继续发消息。
4. `POST /sessions/{id}/confirm` 执行待确认写入；也可调用 `/cancel` 或 `/select/{index}`。

MVP 的 FastAPI 会话状态保存在单进程内存中。多 worker 或生产部署时，将
`InMemorySessionStore` 替换为 Redis，并保留同一会话的串行更新语义。

## 测试

```powershell
python -m pytest -q
```

当前模块测试覆盖 CRUD、预览与二次确认、API、数据文件与查询库同步、确定性意图继承、职责/要求语义拆分、招聘意图过滤、安全改写、AND/OR、异常标点、经验月数抽取、技能标签约束、三层去重、建议分组、中文 JSON 往返编辑、语义 warning/blocking 边界，以及建议确认前后的 Repository 写入边界。测试使用 fake interpreter 或确定性规则，不调用真实外部模型。
